# `track`/edit entries and [history.*]/[settings.history] only take effect
# from mise's global config -- must stay here, not in the root mise.toml.
[dotfiles]
"~/.zshrc" = { mode = "track" }
"~/.zshrc/omz-init" = { block = '''
export ZSH="$HOME/.config/oh-my-zsh"
plugins=(zsh-autosuggestions zsh-syntax-highlighting)
source "$ZSH/oh-my-zsh.sh"
''' }
"~/.zshrc/mise-activate" = { block = 'eval "$(/Users/fabrice.pont/.local/bin/mise activate zsh)"' }

[history.origin]
url = "https://github.com/fapont/dotfiles.git"

[settings.history]
sync = "manual"
